$(window).bind("initialize", function() {
    // initialize patient facet
    // initialize characteristic facet
    // bind get-data event
    $(window).bind("get-data", function() {
        
    });
});